%% Feel free to use, reuse and abuse the code in this file.

{application, session_cookie, [
	{description, "Cowboy application with a session cookie."},
	{vsn, "1"},
	{modules, []},
	{registered, []},
	{applications, [
		kernel,
		stdlib,
		cowboy
	]},
	{mod, {session_cookie_app, []}},
	{env, []}
]}.
